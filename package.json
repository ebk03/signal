{
  "name": "signal",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "client",
    "server",
    "infra"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client"
  }
}
